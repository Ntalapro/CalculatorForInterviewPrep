@{
    ViewData["Title"] = "Result";
}

<div class="container text-center mt-5">
    <h2 class="mb-4">Calculation Result</h2>

    <div class="card shadow-lg p-4 rounded-3">
        <h3 class="display-4 text-success">@Model</h3>
        <p class="text-muted">This is the result of your calculation.</p>
    </div>

    <a href="@Url.Action("Index", "Calculator")" class="btn btn-primary mt-4">← Back to Calculator</a>
</div>
